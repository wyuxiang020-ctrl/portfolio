---
interface Props {
  label: string;
}
const { label } = Astro.props;
---

<span class="inline-block px-3 py-[5px] text-[10px] tracking-[0.15em] uppercase border border-[#d8d8d8] text-[#777] font-sans">
  {label}
</span>
